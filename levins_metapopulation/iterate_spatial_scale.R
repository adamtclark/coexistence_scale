#!/usr/bin/env Rscript

error
rm(list=ls())
#setwd("~/Dropbox/Projects/032_Coexistence_mechanisms/src/levins_metapopulation/")

niterations<-2
scalelst<-c(0.01, 0.02, 0.05, 0.1, 0.3, 0.5, 1)

#matrices for output












#save output (repeat for every scale)